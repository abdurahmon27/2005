---
title: Features
description: Explore the features of the example project
---

# Features

Temporibus autem quibusdam et aut officiis debitis aut rerum necessitatibus saepe eveniet ut et voluptates.

## Core Features

### Fast Performance

Nam libero tempore, cum soluta nobis est eligendi optio cumque nihil impedit quo minus id quod maxime placeat facere possimus.

| Metric | Value | Description |
|--------|-------|-------------|
| Startup | < 100ms | Initial load time |
| Response | < 50ms | Average response |
| Memory | < 50MB | Memory usage |

### Easy Configuration

Omnis voluptas assumenda est, omnis dolor repellendus:

```javascript
const config = {
  // Enable auto-reload
  autoReload: true,

  // Set custom port
  port: 3000,

  // Configure logging
  logging: {
    level: "info",
    format: "json"
  }
};
```

### Plugin System

Itaque earum rerum hic tenetur a sapiente delectus:

- **Core plugins** - Built-in functionality
- **Community plugins** - User-contributed extensions
- **Custom plugins** - Create your own

## Advanced Features

### Middleware Support

```javascript
// Custom middleware example
function customMiddleware(req, res, next) {
  console.log(`Request: ${req.method} ${req.url}`);
  next();
}

app.use(customMiddleware);
```

### Caching

Et harum quidem rerum facilis est et expedita distinctio:

1. **Memory Cache** - Fast, volatile storage
2. **Redis Cache** - Distributed caching
3. **File Cache** - Persistent local storage

### TypeScript Support

Full TypeScript support out of the box:

```typescript
interface Config {
  name: string;
  version: string;
  options?: ConfigOptions;
}

interface ConfigOptions {
  debug: boolean;
  timeout: number;
}

const config: Config = {
  name: "example",
  version: "1.0.0"
};
```

Continue to [API Reference](/docs/example-doc/api-reference) for detailed documentation.
